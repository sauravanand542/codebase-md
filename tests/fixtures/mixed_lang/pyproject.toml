[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mixed-server"
version = "0.1.0"
description = "Backend server for mixed project"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
]
