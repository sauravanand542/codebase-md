# Mixed Language Project

A project with a Python (Flask) backend and JavaScript (React) frontend.

## Setup

- Backend: `pip install -e .` then `python src/server.py`
- Frontend: `npm install` then `npm run dev`
