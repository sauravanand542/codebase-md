# Windsurf Rules — codebase-md

## Project Context

`codebase-md` built with python (cli_tool architecture) with 3 module(s) and 14 dependencies.

## Rules

### Naming & Style
- Naming convention: **mixed**
- Import style: **mixed**
- File organization: **flat**

### Python
- Type hints on all function signatures
- Google-style docstrings on public APIs
- No bare `except:` clauses

## Architecture

**Type:** cli_tool

**Entry Points:**
- `src/codebase_md/cli.py`

**Infrastructure:** CI/CD

## File Structure

- `agents/`
- `docs/`
- `src/` [python]

## Tech Stack

- **Languages:** python
- **pypi:** typer, rich, pydantic, pyyaml, httpx, pytest, pytest-cov, ruff, mypy, types-PyYAML (+4 more)
